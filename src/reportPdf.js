import{jsPDF}from'jspdf';import logo from'./logo.png?inline';import{dateText}from'./data.js';
export function makeScheduleReport({rows,title,period,organization}){const doc=new jsPDF({unit:'mm',format:'a4'}),navy=[7,52,81],cyan=[12,182,175],light=[237,246,245];let y=0,page=0;function header(){page++;doc.setFillColor(...navy).rect(0,0,210,34,'F');doc.addImage(logo,'PNG',12,5,31,24);doc.setTextColor(255).setFont('helvetica','bold').setFontSize(17).text('RELATÓRIO DE ESCALA',50,15);doc.setFont('helvetica','normal').setFontSize(9).text(organization||'Prefeitura de Caxias - MA',50,22);doc.setTextColor(...navy).setFont('helvetica','bold').setFontSize(13).text(title,14,46);doc.setFont('helvetica','normal').setFontSize(9).text(period,14,52);doc.setDrawColor(...cyan).setLineWidth(.8).line(14,56,196,56);y=64}function ensure(h=10){if(y+h>277){doc.setFontSize(7).setTextColor(100).text(`Página ${page}`,196,288,{align:'right'});doc.addPage();header()}}header();if(!rows.length){doc.setTextColor(100).setFontSize(11).text('Nenhuma jornada encontrada para os filtros selecionados.',14,y);return doc}let current='';for(const row of rows){if(current!==row.date){ensure(15);current=row.date;doc.setFillColor(...cyan).roundedRect(14,y-4,182,9,1,1,'F');doc.setTextColor(...navy).setFont('helvetica','bold').setFontSize(9).text(dateText(row.date),18,y+2);y+=12}ensure(13);doc.setFillColor(...light).roundedRect(14,y-4,182,11,1,1,'F');doc.setTextColor(...navy).setFont('helvetica','bold').setFontSize(9).text(row.employee,18,y);doc.setFont('helvetica','normal').setFontSize(8).text(`${row.shift} · ${row.start}–${row.end}`,94,y);doc.text(row.department||'Sem setor',18,y+4.5);doc.text(row.location||'Local não informado',94,y+4.5);y+=14}doc.setFontSize(7).setTextColor(100).text(`Gerado em ${new Date().toLocaleString('pt-BR')} · Página ${page}`,196,288,{align:'right'});return doc}
export function downloadReport(doc,filename){doc.save(filename)}
